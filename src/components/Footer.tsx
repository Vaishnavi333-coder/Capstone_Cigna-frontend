import React from 'react';

export default function Footer() {
  return (
    <footer className="mt-5 text-center text-muted">
      <small>
        InsureConnect Client Portal — a demo client servicing portal
      </small>
    </footer>
  );
}
